{
  "name": "@ch4p/monorepo",
  "version": "0.4.0",
  "description": "Personal AI assistant platform — security-first, multi-channel, programmable",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/ch4p-labs/ch4p.git"
  },
  "homepage": "https://github.com/ch4p-labs/ch4p#readme",
  "bugs": {
    "url": "https://github.com/ch4p-labs/ch4p/issues"
  },
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.29.3",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "vitest",
    "test:coverage": "vitest --coverage",
    "lint": "eslint .",
    "typecheck": "pnpm -r exec tsc --noEmit && tsc --noEmit -p apps/gui/tsconfig.server.json",
    "check": "pnpm -r build && pnpm typecheck && pnpm lint && pnpm vitest run && pnpm audit",
    "bundle": "pnpm -r build && cd apps/cli && bun run build.ts",
    "clean": "pnpm -r clean && rm -rf node_modules"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild"
    ],
    "overrides": {
      "minimatch": ">=10.2.4",
      "rollup": ">=4.59.0",
      "flatted": ">=3.4.2",
      "picomatch": ">=4.0.4",
      "brace-expansion": ">=5.0.5",
      "vite": "^7.3.2"
    }
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^3.0.0",
    "eslint": "^9.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.0.0",
    "vitest": "^3.0.0"
  }
}
